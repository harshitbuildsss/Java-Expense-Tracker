spring.application.name=expensetracker

# 1. Database Connection URL 
spring.datasource.url=jdbc:mysql://localhost:3306/expense_db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC

# 2. MySQL Credentials
spring.datasource.username=root
spring.datasource.password=mysqlHarshit

spring.jpa.hibernate.ddl-auto=update

# Allowed origin(s) for the separately-run React frontend during local development.
# Comma-separated if you need more than one (e.g. a different Vite port).
app.cors.allowed-origins=http://localhost:5173
